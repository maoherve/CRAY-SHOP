<!DOCTYPE html>

<title>ASavoir</title>

{% block body %}
    <h1>ASavoir</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ a_savoir.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ a_savoir.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('a_savoir_index') }}">back to list</a>

    <a href="{{ path('a_savoir_edit', {'id': a_savoir.id}) }}">edit</a>

    {{ include('a_savoir/_delete_form.html.twig') }}
{% endblock %}
