<!DOCTYPE html>

<title>Edit ASavoir</title>

{% block body %}
    <h1>Edit ASavoir</h1>

    {{ include('a_savoir/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('asavoir_index') }}">back to list</a>

    {{ include('a_savoir/_delete_form.html.twig') }}
{% endblock %}
