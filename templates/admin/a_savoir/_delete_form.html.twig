<form method="post" action="{{ path('a_savoir_delete', {'id': a_savoir.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ a_savoir.id) }}">
    <button class="btn">Delete</button>
</form>
