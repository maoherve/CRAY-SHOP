{% extends 'adminlayout.html.twig' %}

{% block header %}
    {{ parent() }}
{% endblock %}

{% block main %}
    <h1 class="text-center">Créer un nouveau champ "Outfit"</h1>
    <div class="text-center">

        <div class="mt-2">{{ include('admin/outfits/_form.html.twig') }}</div>

        <a href="{{ path('admin_outfits_index') }}">Retour</a>
    </div>
{% endblock %}
