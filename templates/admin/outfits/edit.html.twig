{% extends 'adminlayout.html.twig' %}

{% block header %}
    {{ parent() }}
{% endblock %}

{% block main %}
<div class="container container-fluid">
    <h1 class="mt-5 text-center">Edit Outfits</h1>
    <div class="text-center">

        <div class="mt-2">{{ include('admin/outfits/_form.html.twig', {'button_label': 'Update'}) }}</div>

        <div class="mt-2">{{ include('admin/outfits/_delete_form.html.twig') }}</div>

        <a class="mt-2" href="{{ path('admin_outfits_index') }}">back to list</a>
    </div>
</div>
{% endblock %}
