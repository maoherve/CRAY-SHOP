<form method="post" action="{{ path('declinaison_delete', {'id': declinaison.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ declinaison.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
