<div class="container mt-3">
    {% for text in whoAreUs %}
        <h2 class="">
                {{ text.title }}
            </h2>

            <p class="mb-5">
                {{ text.text }}
            </p>
    {% else %}
            <p>ajouter inforlation en tant qu'administrateur</p>
    {% endfor %}
</div>
